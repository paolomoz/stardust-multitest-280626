# Direction — Sony Group Portal

## Active

**Phrase:** "Faithful redesign, modernized" (fully hands-off, single canonical direction, no gate)

**Restatement (dimensional):** Refresh an existing, signal-strong corporate brand portal.
Move *expressive* restrained→committed and *hierarchy/whitespace* toward editorial premium,
while *palette* and *type* stay pinned (Mode A brand-faithful).

**Brand signal:** `signal-strong` — palette has black/white/grey/Sony-blue after clustering
AND a named type family (SST). Mode A active by default; no `--rebrand`.

**Mode:** A (brand-faithful). Palette and type pinned to captured surface.
Image-reuse contract holds. Signature preserved: hero carousel (`heroMedium: image-carousel`).

**Movements:**
- expressive: restrained → committed (SST display scale, editorial whitespace)
- hierarchy: flat-grey → true-black headings
- density: balanced (default; multi-audience hard floor ≤64px section padding)
- ia-fidelity: reimagined (default) — but IA priorities preserved (audience-routing nav, story-led home)
- palette: inherited (monochrome + Sony blue link)
- type: inherited (SST)

**Divergence (brand-faithful mode):**
```
decade           inherited   → 2023-template era, modernized
ground-family    inherited   → stark-white (brand-native)
font deck        inherited   → SST (existing brand stack)
palette          inherited   → black/white/grey + Sony blue
motion register  selected    → editorial (quiet, auto-rotating hero)
```

**Brand-faithful inversions:** retain pure #000/#fff (exempt), retain hex format, retain SST.

**Improvements list:** `stardust/prototypes/home-improvements.md` (5 items — hero headline scale,
hero/news dedup, heading contrast, SST editorial chrome, social band).

**Command sequence:** prototype (home first, then one per template) → migrate → deploy → rollout.

**Assumptions defaulted in:** density balanced (brand register, unanswered); ia-fidelity reimagined
(default); single variant (hands-off mandate).
