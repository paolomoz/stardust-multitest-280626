# Direction — PayPal (US)

## Active

**Phrase:** "faithful redesign, modernized" (autonomous rollout default)

**Resolved:** 2026-06-28

### Mode
**Mode A — brand-faithful.** Captured brand signal is `signal-strong`
(blue palette ladder ≥3 distinct colors + named type families PayPal Open /
PayPal Sans / Plain). Palette and type pinned to the captured surface.
Image-reuse contract holds (captured paypalobjects.com imagery reused at same
semantic position). No `--rebrand`.

### Register
**brand** — inherited from current/PRODUCT.md (US marketing/acquisition site).

### Movements
- expressive: restrained → committed (bigger photographic heroes, confident type)
- distinctiveness: familiar → familiar (faithful — keep PayPal identity)
- tone: serious → reassuring/optimistic (unchanged register)
- density: **balanced** (default; multi-audience hard floor → sectionPadding ≤64px desktop)
- ia-fidelity: **reimagined (default)** — may re-shape section deployment within IA-priority floor
- audience: consumer + business (both preserved; Personal/Business split kept)

### Divergence (brand-faithful mode)
```
decade           inherited   → 2025-now
register         inherited   → brand (product/transactional)
ground-family    inherited   → stark-white (brand-native)
font deck        inherited   → PayPal Open / PayPal Sans
palette          inherited   → captured blue ladder (5 brand blues + 3 neutrals)
motion register  selected    → kinetic-grid (animated stat counter signature)
```

### IA-priority audit
- commercial-conversion → first-viewport (sign-up / get-app / get-card)
- audience-routing → global-header Personal/Business toggle
- trust-affordance → security section present on home

### Improvements list
`stardust/prototypes/home-improvements.md` (5 items: counter-fragment leak,
CTA clutter, missing h1, mixed-case headlines, photography scale).

### Anti-toolbox audit
- Generic-2026-SaaS silhouette → guarded in PRODUCT.md anti-references.
- No logo-as-editorial, no fabricated stats, no flattened signature motion.

### Command sequence
extract ✓ → direct ✓ → prototype (home first, then per-template) → migrate →
deploy/rollout.

### Outputs
PRODUCT.md, DESIGN.md, DESIGN.json (root) + this file.
