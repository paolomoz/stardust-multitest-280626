# Direction — Bank of America

## Active

**Phrase:** "Faithful redesign, modernized."

**Restatement:** Preserve the Bank of America brand identity and IA; raise the
execution to a clean 2026 standard. No rebrand.

**Mode:** A — brand-faithful. Captured brand signal: `signal-strong` (navy, red,
royal blue, link blue; named flagscape logo; clear card-grid IA).

**Movements:**
- expressive: restrained → committed (cleaner hierarchy, bigger benefit numerals)
- distinctiveness: familiar (stays recognisably BofA)
- density: balanced (multi-audience floor → 64px desktop section padding)
- ia-fidelity: faithful (preserve section sequence + content beats; modernize surface)
- palette: inherited (navy #012169, red #e31837, royal blue) — pinned
- typography: inherited grotesk sans — pinned

**Divergence (brand-faithful mode):**
- decade        rolled    → 2025-now
- register      rolled    → civic-institutional
- ground-family inherited → stark-white (brand-native, Mode C override)
- font deck     inherited → existing grotesk sans
- palette       inherited → navy/red/royal-blue set

**IA priorities preserved:** commercial-conversion (first viewport), audience-routing
(header utility nav, locked), trust-affordance (FDIC/security, locked).

**Motion register:** arrival (civic-formal + institutional).

**Improvements list:** stardust/prototypes/home-improvements.md (5 items).

**Templates to prototype (one per archetype):**
1. home (landing)
2. product-landing (cash-back-credit-cards)
3. section-overview / listing (auto-loans, deposits)
4. editorial/article (avoid-bank-scams)

**Command sequence:** prototype home → prototype one per template → lock block names → migrate → deploy → rollout.

**Assumptions defaulted in:** density=balanced (brand multi-audience); single variant (hands-off run).
